[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = services/wallet services/blockchain libs/common/src .
markers =
    unit: pure logic, no infrastructure required
    integration: requires postgres and redis
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra --strict-markers
